# ARLS Targeted UI Cleanup Selector Audit

Worker: `worker-1`
Scope: read-only selector audit + artifact write only
Inputs: `/Users/mark/Desktop/rg-arls-dev/.omx/plans/prd-arls-targeted-ui-cleanup-20260413T0850Z.md`, `/Users/mark/Desktop/rg-arls-dev/.omx/plans/test-spec-arls-targeted-ui-cleanup-20260413T0850Z.md`
Source snapshot: worktree HEAD on 2026-04-13; line numbers refer to the current worker-1 worktree.

## Cleanup plan followed before artifact write

1. Read the PRD/test spec and task scope; keep `.omx/plans/` read-only.
2. Audit only selector families named in the task: workspace tabs, approval tabs, `ui-filterbar`, schedule wizard steppers, KPI/status strips, approval stage flow, and employee/site/detail rails.
3. Do not edit frontend source. Produce this selector map with current source locations, cascade winners/late overrides, recommended delete/neutralize/keep action, and JS/markup hooks.

## Audit method and cascade notes

- Static source search with `rg` and targeted source snippets in `frontend/css/styles.css`, `frontend/index.html`, and `frontend/js/app.js`.
- "Winning" below means the likely static cascade winner by source order, specificity, and `!important`; it is not a rendered CSSOM trace.
- Current `frontend/css/styles.css` contains **1284** `!important` occurrences (`rg -n "!important" frontend/css/styles.css | wc -l`), so cleanup should avoid adding any new undocumented `!important` and should remove/centralize late `!important` overrides where safe.

## Canonical selector map

| Family | Canonical owner to keep | Current markup / JS hooks to preserve | Superseded strategy |
|---|---|---|---|
| Workspace tabs | One global tab grammar around `.workspace-tabs`, `.workspace-tab`, `.workspace-tab.active` in `frontend/css/styles.css` | Markup uses `.workspace-tabs` / `.workspace-tab`, `data-action`, `data-*` route/section hooks, and active/`aria-pressed`/selected state toggled by existing JS. Examples: ops `frontend/index.html:567-594`, leave `frontend/index.html:1868-1907`, attendance `frontend/index.html:3568-3595`, employee/site org tabs `frontend/index.html:9372-9398`. | Delete or neutralize bottom-border-only route patches; keep route wrappers only for layout/visibility, not active-state grammar. |
| Approval tabs | Same global tab grammar plus `.approval-tabs` / `.approval-tab`; avoid `.azure-tabbar` as a competing active treatment | Existing approval queue and leave policy/scope hooks: `frontend/index.html:1418-1439`, `frontend/index.html:1931-1945`, `frontend/index.html:1977-1995`. | Canonicalize `.approval-tabs.azure-tabbar` into the same pill/contained active state; remove bottom-only active borders and blue request-local active colors unless documented as route-specific. |
| `ui-filterbar` / command bars | One command/filter grammar under `.ui-filterbar` plus field normalization tokens `--ui-filter-control-h` and `--ui-filter-control-px` | Existing wrappers: `#requestsFilterBar`/`.requests-filter-controls`, `#leaveRequestsToolbar .leave-requests-filter-controls`, `#attendanceOpsToolbar`, `#employeeToolbar.ui-filterbar`, `#siteFilterGrid.ui-filterbar`, schedule `ui-filterbar`. | Keep semantic wrappers, but neutralize conflicting grid/flex/height patches so fields/buttons share height, baseline, gaps, and responsive wrapping. |
| Schedule wizard steppers | One canonical stepper model for `.schedule-wizard-progress`, `.schedule-wizard-step`, `.active`, `.is-complete` under `#view-schedule` | Static markup in `frontend/index.html:4662-4755`; JS `renderScheduleWizardProgress()` at `frontend/js/app.js:20897-20917` toggles `.active`, `.is-complete`, `aria-pressed`, and `disabled`; `data-step-index` is already available for marker labels. | Keep one schedule stepper section only; remove/neutralize duplicate horizontal/pill/late `!important` density patches. |
| KPI/status strips | One status/KPI strip grammar shared across comparable attendance, leave, requests, reports, schedule, employee/site summary strips | Existing classes: `.attendance-v2-summary-*`, `.attendance-kpi-item`, `.leave-usage-summary-*`, `.requests-kpi-*`, `.reports-finance-submit-summary-*`, `.schedule-upload-summary-*`, `.organization-summary-*`. | Consolidate pastel card variants into neutral surfaces with high-chroma semantic indicators; keep terminology and data source semantics unchanged. |
| Approval stage flow | Approval-flow structure + CSS section around `.hr-approval-stage-flow`, `.hr-approval-stage-card`, `.hr-approval-saved-stage-stack` | JS markup at `frontend/js/app.js:59216-59308` emits `data-stage-order`, `data-member-count`, `.is-interactive`, `.is-saved`, `.is-dropzone`, and member lists. | Keep JS data/order/drag hooks; redesign only CSS/structure needed for diagram flow. Do not change stage order or saved/placeholder behavior without product validation. |
| Employee/site/detail rails | Detail rail grammar around `.employee-directory-*`, `.site-directory-*`, `.organization-summary-*` | Employee DOM `frontend/index.html:10110-10220`, site DOM `frontend/index.html:10541-10595`; employee render helpers `frontend/js/app.js:69469-70510`; site render helpers `frontend/js/app.js:54150-54580`. | Keep DOM/action hooks and inline-rail state classes; canonicalize rail spacing/color/fact cards. Review late hiding rules before deleting columns/summary strips. |

## Selector conflict audit

### 1. Workspace tabs

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Early contained/pill-ish `.workspace-tabs`, `.workspace-tab`, `.workspace-tab.active` inside desktop block: `frontend/css/styles.css:12557-12576`. Shared sizing/typography: `frontend/css/styles.css:14632-14747`. Global Azure-style tab layer: `frontend/css/styles.css:15023-15068`. | The global Azure layer at `15023-15068` wins over earlier contained tab styling for generic `.workspace-tab` because it is later and sets `border: 0`, `border-bottom`, `border-radius: 0`, `background: transparent`, and `.active { border-bottom-color: var(--ui-accent); }`. | **Delete/neutralize** the Azure bottom-border-only active treatment; replace it with the canonical contained/orange grammar. Keep `.workspace-tabbar-row` layout if still needed. | Markup has sufficient hooks: `.workspace-tabs`, `.workspace-tab`, `.active`, `data-action`, `data-route` / `data-section`; examples at `index.html:567-594`, `1868-1907`, `3568-3595`, `9372-9398`. |
| Requests workspace tabs: `frontend/css/styles.css:15196-15230`, `43288-43289`. | `#view-requests #requestsWorkspaceSegments .workspace-tab.active` at `43288-43289` wins for request workspace active border by specificity + later source order. It remains bottom-border-only. | **Neutralize** after canonical global tab grammar is in place; keep request-specific spacing/layout only. | `#requestsWorkspaceSegments`, `data-action` request workspace hooks in markup/JS. |
| Attendance workspace tabs: `frontend/css/styles.css:39212-39214`, `47886-47901`, `51113-51120`. | `#view-attendance #attendanceWorkspaceTabs .workspace-tab.active` at `51118-51120` wins over the earlier blue active at `47899-47901`; it still uses `box-shadow: inset 0 -2px 0 var(--ui-accent)`, i.e. bottom-border-only via shadow. | **Delete/neutralize** the inset bottom-line active treatment; keep any `#attendanceWorkspaceTabs.hidden` visibility rule. | `#attendanceWorkspaceTabs` markup at `frontend/index.html:3568-3595`; existing section switch hooks are sufficient. |
| Leave workspace tabs: `frontend/css/styles.css:17477-17479`, `41120`, `44244-44247`, `49122-49136`. | `#view-leave #leaveWorkspaceTabs .workspace-tab.active` at `49133-49136` wins over earlier blocks; active is still `border-bottom-color` + transparent background. | **Delete/neutralize** bottom-border active; keep route layout and header action row. | `#leaveWorkspaceTabs` markup at `frontend/index.html:1868-1907`, `data-action="leave-workspace-section"`. |
| Schedule workspace tabs: `frontend/css/styles.css:32622-32629`; profile/home/notices route tabs also have route-local active rules (`34862`, `38495`, `41765`). | Schedule active block is contained-ish and not bottom-border-only; home deck tabs still use `box-shadow: inset 0 -2px 0`. | **Keep** schedule block if it matches canonical grammar; **audit/neutralize** home/notices/profile tab bottom-only variants if they are included in the final major-route sweep. | Existing `.workspace-tab` route hooks. |

### 2. Approval tabs

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Baseline `.approval-tabs`/`.approval-tab.active`: `frontend/css/styles.css:2605-2624`. Shared sizing: `14632-14747`. Azure-style approval tabs: `15023-15068`. | `.approval-tabs.azure-tabbar .approval-tab.active` at `15065-15068` wins when `.azure-tabbar` is present and creates bottom-border-only active state. | **Canonicalize** `.approval-tabs.azure-tabbar` to the same contained/orange state as workspace tabs; keep `min-height: 44px`. | Approval queue markup uses `.approval-tabs azure-tabbar` and `.approval-tab active` at `frontend/index.html:1418-1439`; leave policy/scope tabs at `1931-1945`, `1977-1995`. |
| Requests secondary tabs: `frontend/css/styles.css:43776-43780`. | Later request-specific rule wins for `#requestsSecondaryTabs .approval-tab.active`, using blue border/background/color, not orange. | **Neutralize** blue route-specific active color unless intentionally documented; replace with orange canonical active. | `data-action="approval-queue-tab"`, `data-tab` hooks remain enough. |
| Leave policy/grants/secondary tabs: `frontend/css/styles.css:44279-44296`, late `56290-56302` with `!important`. | `#view-leave #leavePolicyStatusTabs .approval-tab.active` at `56298-56302` wins because it is later and uses `!important`. It is pill-like, but the `!important` block only covers policy status tabs, not grants/secondary. | **Keep temporarily as canonical shape reference but move into the global approval-tab grammar and remove `!important`**. Also canonicalize `leaveGrantsTabs` and `leaveWorkspaceSecondaryTabs`; they currently rely on earlier bottom-border block at `44294-44296`. | Existing leave tab buttons carry `data-action` and `data-tab`/`data-scope`. |
| Attendance approval tabs: `frontend/css/styles.css:15196-15230`, `22167-22180`. | Active color is inherited from global/route-specific bottom-border Azure rules when `#attendanceApprovalTabs.approval-tabs.azure-tabbar` is used. | **Neutralize** with global approval tab canonical owner. | No new markup hook required. |

### 3. `ui-filterbar` / filter controls

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Canonical-ish global filterbar: `frontend/css/styles.css:11295-11350`, including `--ui-filter-control-h: 44px`, field label normalization, and button/input/select height. | This block wins only where no later route-specific grid/flex or height override conflicts. It already includes employee/leave/attendance field classes in the grouped selectors. | **Keep as canonical owner** and expand deliberately if needed. | Markup has `.ui-filterbar` on ops `index.html:617`, schedule `3026`, attendance mobile `4237`, schedule calendar `4459`, employee `9437`, site `10389`. |
| Requests filter controls: `frontend/css/styles.css:15747-15756`, then layout rewrite `17148-17416`, mobile `17389-17404`, and `40800`. | `17148-17416` wins over the earlier nine-column grid and creates a three-cluster layout; `40800` may win for a nested `#requestsFilterBar .requests-filter-controls` subset. | **Neutralize duplicate layout layers** after selecting a canonical responsive grid/flex pattern. Keep semantic cluster classes if they encode date/core/search grouping. | Existing `.requests-filter-controls`, `.requests-filter-cluster-*`, `data-action="requests-open-employee-filter"` hooks. |
| Leave request filters: `frontend/css/styles.css:17605-17643`, `38936-39015`, `44475`, `49177`, `50412-50418`, `52408`, `53615`, `53964-53990`, `54644`. | Latest non-media general rule at `53964-53990` wins and changes `.leave-requests-filter-controls` to `display:flex` with inline-flex range fields; it overrides earlier grid/autofit layout at `17605`. | **Canonicalize** leave toolbar under `.ui-filterbar`; neutralize the late flex rewrite if it causes baseline/width drift. Keep `leave-filter-field-range` only if range controls need inline grouping. | `#leaveRequestsToolbar .leave-requests-filter-controls` markup at `frontend/index.html:1977-2028` and `2458-2465`; employee picker hooks via `data-action="leave-open-employee-filter"`. |
| Attendance toolbar: `frontend/css/styles.css:18963-19040`, `19860-19926`, `20377-20548`, `39228-39734`, `45705-45752`, `46849-46913`, `47930`, `50184`. | Latest route blocks around `46849-46913` and `50184` win for `.attendance-ops-toolbar-main/group` layout, while global field heights at `11295-11350` still apply where not overridden. | **Keep wrapper-specific grouping**, but canonicalize height/baseline/button rhythm through the filterbar token group; delete duplicated toolbar group sizing once one layout is selected. | `#attendanceOpsToolbar` markup at `frontend/index.html:3605-3755`; hooks include `data-action="attendance-open-range-sheet"`, `attendance-open-employee-filter`, `attendance-open-site-filter`. |
| Employee/site filter bars: employee old blocks `2993-3013`, `9973-10015`, `10378-10428`; global inclusion `11309-11346`; site filter `2668`, `9590`, `10946`. | For employee fields, global `11309-11346` wins on field heights over earlier `9973-10428`, while route-specific visibility/toggle blocks at `43456-43488` and late `56304-56312` hide pieces. Site filter grid has fewer late conflicts but shares `.ui-filterbar`. | **Keep** `#employeeToolbar.ui-filterbar` and `#siteFilterGrid.ui-filterbar`; **review** late hidden elements before considering deletion because they may be intentional density decisions. | Employee toolbar markup `frontend/index.html:9437-9488`; site toolbar markup `frontend/index.html:10389-10449`. |

### 4. Schedule wizard steppers

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Base vertical rail: `frontend/css/styles.css:6257-6356`; mobile adjustment `7346-7356`. | Wins only before later `#view-schedule` scoped stepper blocks. Good concepts: `data-step-index` markers, `.active`, `.is-complete`, connector after pseudo-element that does not pierce circles. | **Keep as reference only** if the final design chooses a vertical rail; otherwise delete/neutralize once the route-scoped canonical stepper is chosen. | Static stepper markup at `frontend/index.html:4662-4755` contains `#scheduleBaseWizardProgress`, `#scheduleHqWizardProgress`, `.schedule-wizard-step`, `data-step`, and `data-step-index`. |
| Route-scoped horizontal stepper: `frontend/css/styles.css:48575-48641`; duplicate at `55281-55347`. | Duplicate `55281-55347` wins over `48575-48641` due later source order for the same selectors. | **Keep one copy only** if horizontal is canonical; delete the earlier duplicate block to reduce cascade debt. | `renderScheduleWizardProgress()` at `frontend/js/app.js:20897-20917` already toggles `.active` and `.is-complete`. |
| Late pill stepper: `frontend/css/styles.css:54675-54726`. | This block is superseded by later duplicate horizontal at `55281-55347`, except any properties not reset by the later block can leak. It changes connector behavior to `display:none`. | **Delete/neutralize**; it conflicts with the PRD's connector requirement and turns the stepper into pills. | No JS hook needed; avoid adding new behavior. |
| Final density patch: `frontend/css/styles.css:56029-56041` with `!important` gap/min-height/padding/font size. | This wins for gap and step density because of `!important`, layered on top of the horizontal stepper at `55281-55347`. | **Neutralize/remove `!important`** after canonical stepper spacing is finalized. It is a late override and should not remain as the final owner. | Existing `data-step-index` is enough for marker labels; if a new connector wrapper is needed, add it in `renderScheduleWizardProgress` only after CSS-only reconciliation fails. |

### 5. KPI / status strips

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Attendance status summaries: early card grid at `frontend/css/styles.css:29220-29362`, additional variants `29908-30059`, `30601-30887`, daily strip `45028-45049`, global law `50045-50125`, active/semantic card accents `51248-51320`, and late Shople/period pass `55700-55885+`. | The latest Shople-like block at `55700-55885` uses `!important` and wins for status/period strips it targets; `51248-51320` still wins for active/semantic cases not covered by the later block. This creates mixed card/rail treatments. | **Canonicalize** one attendance summary grammar. Prefer removing `!important` rail overrides after the final grammar is authored; preserve high-chroma semantic accents without pastel-dominant card fills. | Existing classes expose tone state (`.is-success`, `.is-danger`, `.is-warning`, `.is-accent`, `.is-active`) so no JS hook is required. |
| Attendance legacy KPI strip: `frontend/css/styles.css:18599-18621`, `19000-19032`, `20017-20063`, `39418-39447`, `39823-39836`. | Route-scoped later blocks around `39418-39447` win for `#view-attendance .attendance-kpi-item`; older neutral/pastel backgrounds remain for less-specific uses. | **Merge** into the canonical status strip or document as legacy-only; do not let `.attendance-kpi-item.active` diverge from `.attendance-v2-summary-item.is-active`. | Existing `data-tone` and `.active` hooks. |
| Leave usage summary: `frontend/css/styles.css:17700-17723`, `38703-38708`, `38991`, `44324-44339`, `49147-49153`. | `49147-49153` wins for targeted first-child/border styling; `44324-44339` wins for most item typography. | **Canonicalize** to status strip grammar; keep if it matches neutral surface + high-chroma indicator, otherwise neutralize pastel/first-child exceptions. | Existing `.leave-usage-summary-strip/item/label/value`; no JS hook required. |
| Requests KPI strip: `frontend/css/styles.css:15333-15362`, `15761`, `17244-17257`, `22267-22280`, `38707-38717`. | Later request-specific rules at `38707-38717` and route-scoped blocks win for active item treatment. | **Merge** with canonical status strip; keep requests-specific count semantics. | `.requests-kpi-item.is-active`, label/value classes. |
| Reports finance summary and schedule upload summary: finance repeated at `32889-32909`, `33732-33777`, `42337-42348`, `48747-48778`, `52716-52747`, `54976-55499`; schedule upload at `6424-6448`, `6672-6678`, `6723`, `54753`, `54807-54811`. | Multiple finance blocks repeat same selectors; the latest around `55468-55499` wins where present. Schedule upload later `54807-54811` wins for route-scoped summary strip/card. | **Deduplicate** repeated finance summary sections and align with schedule/attendance status strip grammar while preserving wizard workflow. | Existing summary item classes are adequate. |
| Employee/site organization summary: `frontend/css/styles.css:9709-9892`, employee-specific `10086-10143`, site/dark `22955-23007`, late `56304-56318` hides employee summary/address column. | Late `56304-56318` uses `!important` to hide the employee summary strip and site address column; dark theme block wins in dark mode for summary/fact card surfaces. | **Keep** summary/fact hook classes; **review and document** late hidden rules. If the cleanup requires detail/list balance, neutralize hidden address/summary rules only after confirming no density requirement depends on them. | Summary renderer hooks in JS; site `renderSiteDirectorySummaryStrip()` at `frontend/js/app.js:54648-54771`. |

### 6. Approval stage flow

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Earlier HR saved-stage alignment patch: `frontend/css/styles.css:52486-52533`. | Later full flow block at `54093-54440` wins for most `.hr-approval-*` selectors; the earlier patch is effectively redundant where selectors overlap. | **Delete/neutralize** earlier duplicate alignment patch after verifying no unique rule remains. | Existing JS emits saved and interactive state classes. |
| Current flow/diagram CSS: `frontend/css/styles.css:54093-54440`. | This is the main active owner for `.hr-approval-saved-stage-stack`, `.hr-approval-stage-flow`, `.hr-approval-stage-card`, `.hr-approval-stage-topbar`, body/member list, empty state, and drag-over state. | **Keep and refine** as canonical approval-flow owner. Convert to the final clean diagram/flow here rather than adding a later override block. | `buildHrApprovalStageCardMarkup()` at `frontend/js/app.js:59216-59282` emits `section.hr-approval-stage-flow`, `data-stage-order`, `data-member-count`, `.is-saved`, `.is-interactive`, `.is-dropzone`, and `--saved-stage-max-width`. |
| Saved stack renderer: `frontend/js/app.js:59284-59308`. | Renderer currently sorts with `.sort((left, right) => right.stageOrder - left.stageOrder)`, so visual order may be descending. This is a behavior/meaning risk. | **Keep unless product owner confirms a stage-order correction.** If visual diagram expects ascending flow, change only with explicit validation and tests. | `renderHrApprovalProcedureSavedStack()` and shell state placeholder at `59310+`; drag/drop/action hooks around `100905+`. |

### 7. Employee/site/detail rails

| Current source locations | Winning cascade / late override | Action | JS / markup hooks |
|---|---|---|---|
| Employee rail layout: `frontend/css/styles.css:19245-19864`; inline rail route layout at `13986-14040`; desktop inline drawer at `19769-19851`; detail tab sizing active state at `22169-22180`. | Base employee drawer styles at `19245-19864` own most rail components; inline-rail media block at `19769-19851` wins for desktop inline rail; `22169-22180` wins for compact tab sizing/color but does not remove earlier bottom-border active line at `19420-19423`. | **Keep** rail structure and state classes; canonicalize fact/KPI/feed cards to new detail-rail grammar. **Neutralize** detail tab bottom-border-only active if it remains visually aligned with old tab grammar. | DOM: `frontend/index.html:10110-10220`. JS: `buildEmployeeDirectoryFactGrid()` at `69469-69490`, KPI cards at `69627-69680`, header and tab renderers at `70054-70510`. |
| Employee filter/action rail: CSS `9973-10015`, `10378-10428`, global field normalization `11309-11346`, late hidden controls `56304-56312`. | Global filterbar wins on field heights; late `#view-employees #employeeDirectorySummaryStrip { display:none !important; }` and hidden bulk/hint controls win regardless of route context. | **Review hidden controls as product/density exceptions** before removal. Do not delete action hooks; if visual cleanup needs fewer boxes, restyle rather than remove actions. | `#employeeToolbar.ui-filterbar`, `#employeeTopActionBar`, and action buttons in `frontend/index.html:9406-9488`. |
| Site detail rail: base CSS `frontend/css/styles.css:10642-10861`; responsive/inline rail `10969-11194`; dark theme `22955-23007`; late address-column hide `56316-56318`. | Base site detail panel/fact card styles win, then inline-rail media rules win for desktop rail layout. The late `56316-56318` `!important` hides the address column regardless of detail-rail layout. | **Keep** detail panel hooks and fact card classes; **review** address-column hide as likely cleanup debt before final detail/table balance pass. | DOM: `frontend/index.html:10541-10595`. JS: `setSiteDirectoryDetailOpen()` at `54186-54241`, `buildSiteDirectoryFactGrid()` at `54284-54313`, `renderSiteDirectoryDetail()` at `54483-54580`. |
| Organization summary strip/fact cards shared by employee/site: `frontend/css/styles.css:9709-9892`, employee-specific `10086-10143`, dark site/fact card `22955-23007`. | Employee-specific summary rules at `10086-10143` win for employee view; dark mode block wins in dark mode. Late hide rule at `56304` suppresses employee summary strip entirely. | **Keep** shared summary/fact card vocabulary but align color tokens with KPI/status strip grammar. Remove hide only after route verification confirms summary strip should be visible. | `renderOrganizationSummaryStrip()` callers for employee/site; site summary renderer `frontend/js/app.js:54648-54771`. |

## Recommended implementation sequencing for downstream workers

1. **Tabs first:** Replace `15023-15068` Azure bottom-border active grammar with one contained/orange active state; then delete route-specific bottom-border/inset shadow patches for requests, attendance, and leave.
2. **Filterbar second:** Keep global `11295-11350`; migrate requests/leave/attendance/employee/site wrappers to shared field/button height and baseline. Remove only layout duplicates after verifying each route wraps at 375/768/desktop.
3. **Stepper third:** Choose one schedule stepper model; delete the duplicate `48575-48641` or `55281-55347`, remove pill block `54675-54726`, and fold `56029-56041` density into the canonical block without `!important`.
4. **KPI/status strips fourth:** Merge attendance v2/legacy, leave usage, requests KPI, finance summary, schedule upload summary, and organization summary into a neutral strip grammar with high-chroma semantic indicators.
5. **Approval flow + detail rails fifth:** Refine the existing HR approval-flow CSS owner (`54093-54440`) and employee/site rail blocks rather than appending new late overrides. Preserve JS data/action hooks.
6. **Final CSS integration:** One owner should run the grep assertions from the test spec and ensure no new bottom-border-only active global tabs or undocumented `!important` are introduced.

## Open risks / handoff notes

- `renderHrApprovalProcedureSavedStack()` sorts saved stages descending in `frontend/js/app.js:59295`; if the desired diagram reads first-to-last left/top-to-right, this is a behavior-facing change and should be explicitly validated.
- Several route-specific "Shople" and "global design law" retrofit blocks use `!important` extensively. Future workers should avoid adding another late override layer; consolidate into the earlier canonical owner sections instead.
- Employee/site late hide rules at `frontend/css/styles.css:56304-56318` may be intentional density shortcuts or cleanup debt. Treat them as review-required, not automatic deletes.
